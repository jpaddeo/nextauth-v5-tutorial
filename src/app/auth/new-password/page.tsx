import NewPasswordForm from '@/components/auth/new-password-form';

type NewPasswordPageProps = {};

export default function NewPasswordPage({}: NewPasswordPageProps) {
  return <NewPasswordForm />;
}
